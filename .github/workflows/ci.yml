name: CI

on:
  push:
    branches: [main, develop]
  pull_request:

concurrency:
  group: ci-frappe_microsoft365-${{ github.ref }}
  cancel-in-progress: true

jobs:
  tests:
    name: Server (install + migrate + tests)
    runs-on: ubuntu-latest

    services:
      mariadb:
        image: mariadb:10.6
        env:
          MARIADB_ROOT_PASSWORD: root
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mariadb-admin ping --host=localhost --password=root"
          --health-interval=5s --health-timeout=2s --health-retries=10

    steps:
      - name: Clone
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "18"

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y mariadb-client redis-server libcups2-dev

      - name: Install frappe-bench
        run: pip install frappe-bench

      - name: Init bench + Frappe (stable)
        run: |
          bench init --skip-redis-config-generation --skip-assets \
            --frappe-branch version-15 --python "$(which python)" ~/frappe-bench

      - name: Configure MariaDB charset
        run: |
          mariadb --host 127.0.0.1 --port 3306 -u root -proot -e "SET GLOBAL character_set_server = 'utf8mb4'"
          mariadb --host 127.0.0.1 --port 3306 -u root -proot -e "SET GLOBAL collation_server = 'utf8mb4_unicode_ci'"

      - name: Get app + create site + install
        working-directory: /home/runner/frappe-bench
        env:
          CI: "Yes"
        run: |
          bench get-app frappe_microsoft365 "${GITHUB_WORKSPACE}"
          bench new-site test_site \
            --mariadb-root-password root --admin-password admin \
            --no-mariadb-socket --db-host 127.0.0.1 --db-port 3306
          bench --site test_site install-app frappe_microsoft365
          bench --site test_site migrate

      - name: Run tests
        working-directory: /home/runner/frappe-bench
        run: |
          bench --site test_site set-config allow_tests true
          bench --site test_site run-tests --app frappe_microsoft365
